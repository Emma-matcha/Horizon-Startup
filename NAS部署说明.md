# 黑群晖 NAS 部署说明

NAS 管理地址：`http://10.0.0.2:5000`

DSM 的 `5000` 端口是管理入口，本网站使用 `8088`，避免与 DSM 冲突。

## 1. 上传项目

在 DSM 的 File Station 中创建：

```text
/volume1/docker/red-house
```

将整个项目上传到该目录。务必确认离线语音模型存在：

```text
public/models/vosk-model-small-cn-0.22.tar
```

## 2. 用 Container Manager 构建

1. 从“套件中心”安装并打开“Container Manager”。
2. 进入“项目”，点击“新增”。
3. 项目名称填写 `red-house`。
4. 路径选择 `/volume1/docker/red-house`。
5. 选择使用目录中的 `docker-compose.yml`。
6. 完成向导，等待镜像构建和容器启动。

启动后可以先检查：

```text
http://10.0.0.2:8088
```

这个 HTTP 地址只能用于确认页面已启动。浏览器麦克风要求安全上下文，完整语音功能必须通过 HTTPS 使用。

## 3. 配置群晖反向代理与 HTTPS

准备一个解析到 `10.0.0.2` 的局域网域名，例如：

```text
vision.home.example.com
```

在 DSM 的“控制面板 → 登录门户 → 高级 → 反向代理”中新建规则：

| 项目 | 值 |
| --- | --- |
| 来源协议 | HTTPS |
| 来源主机名 | `vision.home.example.com` |
| 来源端口 | `443` |
| 目标协议 | HTTP |
| 目标主机名 | `127.0.0.1` |
| 目标端口 | `8088` |

然后在“控制面板 → 安全性 → 证书”中，为该域名导入或申请可信证书，并把证书分配给反向代理服务。

正式访问地址：

```text
https://vision.home.example.com
```

如果只在家里使用，不需要将 `5000` 或 `8088` 映射到公网。可以通过路由器、本地 DNS 或 AdGuard Home 将上述域名解析到 `10.0.0.2`。

## 4. 公网域名和端口映射

如果需要从外网访问：

1. 在域名服务商处增加一条 `A` 记录，将网站域名解析到家庭宽带的公网 IPv4；如果公网地址会变化，先在 DSM 中配置 DDNS，再把自有域名用 `CNAME` 指向 DDNS 域名。
2. 在路由器中只建立下面这一条端口映射：

   ```text
   公网 TCP 443 → 10.0.0.2 TCP 443
   ```

3. 不要把 DSM 管理端口 `5000/5001` 或容器端口 `8088` 映射到公网。
4. 在 DSM 反向代理中使用公网域名作为“来源主机名”，目标仍为 `127.0.0.1:8088`。
5. 为公网域名申请 Let's Encrypt 或导入其他可信 HTTPS 证书，并分配给该反向代理规则。
6. 使用手机关闭 Wi-Fi，通过移动网络访问 `https://你的域名`，确认公网解析、证书和反向代理都正常。

如果家庭宽带没有公网 IPv4、处于运营商 CGNAT 后面，普通路由器端口映射不会生效。这种情况下需要向运营商申请公网地址，或改用受控隧道/VPN 方案。

## 5. 验证

在 HTTPS 页面打开 Chrome 开发者工具，在 Console 中运行：

```js
window.isSecureContext
window.crossOriginIsolated
```

两项都应该返回 `true`。然后检查浏览器是否弹出麦克风授权，并完成一次方向语音测试。

## 更新网站

上传新版本文件后，在 Container Manager 中选择 `red-house` 项目，执行“构建”，然后重新启动项目。

## 数据说明

筛查结果保存在访问设备的浏览器 `localStorage` 中，而不是 NAS 容器里。重新构建容器不会删除浏览器中的记录，但清除浏览器网站数据会删除记录。
